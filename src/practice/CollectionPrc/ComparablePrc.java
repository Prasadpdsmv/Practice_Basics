package practice.CollectionPrc;

public class ComparablePrc implements Comparable {
    @Override
    public int compareTo(Object o) {
        return 0;
    }

    // if have variable to validate

    // if you want to extend more on
}
